-r requirements.txt
# The competition documents do not pin a PyTorch version. This range is only
# for the local trainable smoke baseline; in the platform container prefer the
# preinstalled approved PyTorch/CUDA combination.
torch>=2.2,<3
